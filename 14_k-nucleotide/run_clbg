#!/bin/sh

SRC_DIRS="scribble original"
INPUTS="input_1.fasta input_2.fasta input_3.fasta input_4.fasta"

for SRC_DIR in $SRC_DIRS; do
    echo > ./$SRC_DIR.time
    cd $SRC_DIR
    go build
    cd ..
done

for SRC_DIR in $SRC_DIRS; do
    for INPUT in $INPUTS; do
        for NCPU in $(seq 1 16); do
            for ITER in $(seq 1 20); do
                echo "($ITER of 20) ./$SRC_DIR/$SRC_DIR -ncpu $NCPU < ./input/$INPUT >> ./$SRC_DIR.time"
                ./$SRC_DIR/$SRC_DIR -ncpu $NCPU < ./input/$INPUT >> ./$SRC_DIR.time
                tail -1 ./$SRC_DIR.time
            done
        done
    done
done
